rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /chatroom/room1/messages/{document} {
      allow read:  if true;
      allow write: if request.auth.uid != null;
    }
  }
}
